triangle:
  build: triangle

antenna:
  build: antenna
  links:
    - triangle:triangle
  volumes:
    - antenna:/usr/src/app
  ports:
    - 5000:5000
  environment:
    CHIKKA_CLIENT_ID:
    CHIKKA_SECRET_KEY:
    CHIKKA_SHORTCODE:

